import { Pencil, Trash2 } from "lucide-react";
import type { AdminJob, JobStatus } from "../../services/adminApi";

export default function AdminJobTable({ jobs, onEdit, onStatus, onDelete, deletingId }: { jobs: AdminJob[]; onEdit: (job: AdminJob) => void; onStatus: (id: number, status: JobStatus) => void; onDelete: (id: number) => void; deletingId?: number | null }) {
  return <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="hidden overflow-x-auto md:block"><table className="min-w-full text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50"><tr>{["Job", "Location", "Applications", "Status", "Updated", "Actions"].map((head) => <th key={head} className="px-5 py-3 text-xs font-semibold uppercase tracking-wider text-slate-500">{head}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{jobs.map((job) => <tr key={job.id} className="align-top"><td className="px-5 py-4"><p className="font-semibold text-slate-900">{job.title}</p><p className="mt-1 text-xs text-slate-500">{job.company}</p></td><td className="px-5 py-4 text-slate-600">{job.location}</td><td className="px-5 py-4 font-semibold text-slate-700">{job._count.applications}</td><td className="px-5 py-4"><select value={job.status} onChange={(e) => onStatus(job.id, e.target.value as JobStatus)} className="rounded-lg border border-slate-200 px-2.5 py-2 text-xs font-semibold outline-none"><option>DRAFT</option><option>ACTIVE</option><option>CLOSED</option></select></td><td className="px-5 py-4 text-xs text-slate-500">{new Date(job.updatedAt).toLocaleDateString()}</td><td className="px-5 py-4"><div className="flex gap-2"><button title="Edit" onClick={() => onEdit(job)} className="rounded-lg border border-slate-200 p-2 text-slate-600 hover:border-blue-200 hover:text-blue-600"><Pencil size={15} /></button><button title="Delete" onClick={() => onDelete(job.id)} disabled={deletingId === job.id || job._count.applications > 0} className="rounded-lg border border-slate-200 p-2 text-slate-600 hover:border-red-200 hover:text-red-600 disabled:cursor-not-allowed disabled:opacity-30"><Trash2 size={15} /></button></div></td></tr>)}</tbody></table></div><div className="divide-y divide-slate-100 md:hidden">{jobs.map((job) => <div key={job.id} className="p-4"><div className="flex items-start justify-between gap-4"><div><p className="font-semibold text-slate-900">{job.title}</p><p className="mt-1 text-xs text-slate-500">{job.company} · {job.location}</p></div><span className="text-xs text-slate-500">{job._count.applications} apps</span></div><div className="mt-4 flex items-center gap-2"><select value={job.status} onChange={(e) => onStatus(job.id, e.target.value as JobStatus)} className="flex-1 rounded-lg border border-slate-200 px-2.5 py-2 text-xs font-semibold"><option>DRAFT</option><option>ACTIVE</option><option>CLOSED</option></select><button onClick={() => onEdit(job)} className="rounded-lg border border-slate-200 p-2 text-slate-600"><Pencil size={15} /></button><button onClick={() => onDelete(job.id)} disabled={deletingId === job.id || job._count.applications > 0} className="rounded-lg border border-slate-200 p-2 text-red-600 disabled:opacity-30"><Trash2 size={15} /></button></div></div>)}</div></div>;
}
