import { ExternalLink } from "lucide-react";
import type { AdminApplication, ApplicationStatus } from "../../services/adminApi";

const statusClass: Record<ApplicationStatus, string> = { APPLIED: "bg-sky-50 text-sky-700", SHORTLISTED: "bg-amber-50 text-amber-700", INTERVIEW: "bg-violet-50 text-violet-700", SELECTED: "bg-emerald-50 text-emerald-700", REJECTED: "bg-red-50 text-red-700" };

export default function AdminApplicationTable({ applications, onStatus, onDelete }: { applications: AdminApplication[]; onStatus: (id: number, status: ApplicationStatus) => void; onDelete: (id: number) => void }) {
  return <div className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="hidden overflow-x-auto md:block"><table className="min-w-full text-left text-sm"><thead className="border-b border-slate-200 bg-slate-50"><tr>{["Candidate", "Job", "Contact", "Status", "Resume", "Applied"].map((head) => <th key={head} className="px-5 py-3 text-xs font-semibold uppercase tracking-wider text-slate-500">{head}</th>)}</tr></thead><tbody className="divide-y divide-slate-100">{applications.map((item) => <tr key={item.id}><td className="px-5 py-4"><p className="font-semibold text-slate-900">{item.name}</p><p className="mt-1 text-xs text-slate-500">{item.qualification || "Qualification not provided"}</p></td><td className="px-5 py-4"><p className="font-medium text-slate-800">{item.job.title}</p><p className="mt-1 text-xs text-slate-500">{item.job.company} · {item.job.location}</p></td><td className="px-5 py-4"><p className="text-xs text-slate-700">{item.email}</p><p className="mt-1 text-xs text-slate-500">{item.phone}</p></td><td className="px-5 py-4"><select value={item.status} onChange={(e) => onStatus(item.id, e.target.value as ApplicationStatus)} className={`rounded-full border-0 px-3 py-2 text-xs font-semibold outline-none ${statusClass[item.status]}`}><option>APPLIED</option><option>SHORTLISTED</option><option>INTERVIEW</option><option>SELECTED</option><option>REJECTED</option></select></td><td className="px-5 py-4">{item.resumeUrl ? <a href={item.resumeUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-1 text-xs font-semibold text-blue-600 hover:text-blue-700">View resume<ExternalLink size={13} /></a> : <span className="text-xs text-slate-400">Unavailable</span>}</td><td className="px-5 py-4 text-xs text-slate-500">{new Date(item.createdAt).toLocaleDateString()}</td></tr>)}</tbody></table></div><div className="divide-y divide-slate-100 md:hidden">{applications.map((item) => <div key={item.id} className="p-4"><div className="flex justify-between gap-3"><div><p className="font-semibold text-slate-900">{item.name}</p><p className="mt-1 text-xs text-slate-500">{item.job.title}</p></div><span className="text-[11px] text-slate-400">{new Date(item.createdAt).toLocaleDateString()}</span></div><p className="mt-3 text-xs text-slate-500">{item.email} · {item.phone}</p><div className="mt-4 flex items-center gap-2"><select value={item.status} onChange={(e) => onStatus(item.id, e.target.value as ApplicationStatus)} className={`flex-1 rounded-full border-0 px-3 py-2 text-xs font-semibold ${statusClass[item.status]}`}><option>APPLIED</option><option>SHORTLISTED</option><option>INTERVIEW</option><option>SELECTED</option><option>REJECTED</option></select>{item.resumeUrl && <a href={item.resumeUrl} target="_blank" rel="noreferrer" className="rounded-lg border border-slate-200 p-2 text-blue-600"><ExternalLink size={15} /></a>}<button onClick={() => onDelete(item.id)} className="rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-red-600">Delete</button></div></div>)}</div></div>;
}
